UPDATE GITHUB REMOTE URL INSTRUCTIONS
====================================

The deployment is failing because the GitHub repository URL is not correctly configured.

1. FIRST, check your actual GitHub repository URL:
   - Go to your GitHub account
   - Find the PersonalWebsite repository
   - Copy the complete URL (e.g., https://github.com/yourusername/repository-name.git)

2. THEN, update the remote URL with this command:
   cd c:\Users\tusha\Desktop\Webite
   git remote set-url origin YOUR_ACTUAL_REPOSITORY_URL

3. FINALLY, push the changes:
   git push origin main

This will trigger a new deployment on Render with the fixed build configuration.